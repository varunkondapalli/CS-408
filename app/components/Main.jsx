var React = require('react');

var Main = React.createClass({
  render: function () {
    return(
      <div>

        <div>
      <button className="hollow button">Logout</button>
      <h1>Ball'r Leagues</h1>
      </div>
      </div>

    );
  }
});

module.exports = Main;
