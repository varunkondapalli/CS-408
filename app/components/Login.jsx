import React from 'react';

export var Login = React.createClass({
  render(){
    return (
      <div>
        <h1>Ball'r Leagues</h1>
        <div>
        <h3>Login</h3>
      <button className="button">Login with Google</button>
      </div>
      </div>
    );
  }
});

export default Login;
